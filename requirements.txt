# =========================
# 문서-Markdown 변환 에이전트
# =========================

# 1. LLM 및 API 연동 (AI Agent Core)
openai==1.14.0          # GPT-4o 등 OpenAI 모델 호출용
anthropic==0.21.3       # Claude 3.5 모델 호출용
python-dotenv==1.0.1    # .env 파일에서 API 키를 안전하게 불러오기 위함

# 2. 문서 전처리 및 시각 구조 분석 (Input Layer & Visual Analysis)
marker-pdf==0.3.2       # 제안서에서 권장한 최신 PDF/Layout 분석 오픈소스
pdf2image==1.17.0       # PDF 페이지를 고화질 이미지로 변환 (Vision 모델 입력용)
PyMuPDF==1.24.0         # PDF 내의 텍스트, 메타데이터, 이미지 좌표(Bounding Box) 추출
unstructured==0.12.5    # 복잡한 문서 구조(표, 다단 등) 전처리를 돕는 필수 라이브러리

# 3. 이미지 처리 및 컴퓨터 비전 (Vision AI)
Pillow==10.2.0          # 파이썬 기본 이미지 처리 라이브러리 (크롭, 리사이즈 등)
opencv-python==4.9.0    # 딥러닝 모델 적용 전 이미지 보정 및 윤곽선(표 테두리) 탐지

# 4. 데이터 조작 및 표(Table) 처리
pandas==2.2.1           # 추출된 표 데이터를 데이터프레임으로 변환 후 Markdown으로 출력하기 위함
numpy==1.26.4           # 이미지 행렬 연산 및 데이터 처리 기본 라이브러리

# 5. 사용자 인터페이스 (Output Layer & UI)
streamlit==1.32.2       # 원본 문서와 변환된 MD 프리뷰를 대조/편집하는 웹 대시보드 구축용

# 6. 평가 지표 (Benchmark / Evaluation)
editdistance==0.8.1     # 원본 텍스트와 변환된 텍스트 간의 일치율(보존율) 측정
nltk==3.8.1             # 자연어 처리 기반 텍스트 유사도 검증